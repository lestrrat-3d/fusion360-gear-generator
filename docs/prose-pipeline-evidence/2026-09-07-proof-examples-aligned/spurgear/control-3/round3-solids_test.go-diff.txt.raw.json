{
  "schema": 1,
  "encoding": "utf8-chunks",
  "original_name": "round3-solids_test.go-diff.txt",
  "original_sha256": "3e3e1ae8a01cd0fbd3c2983b585e8862796a6b7a826b0cc37c9aa2e156c597f2",
  "original_bytes": 6232,
  "chunks": [
    "--- round2/solids_test.go\n+++ round3/solids_test.go\n@@ -39,25 +39,58 @@\n func stepExtrudeTooth(t *te",
    "sting.T, doc *decad.Document, p map[string]float64) []*decad.Body {\n \ts := proofkit.NewSketch(t)\n \tb",
    "uildGearSketch(t, s, p)\n-\tfor _, profile := range s.Profiles() {\n-\t\tif len(profile.Outer) > 2 {\n-\t\t\t",
    "return []*decad.Body{extrude(t, doc, s, profile, p)}\n-\t\t}\n-\t}\n-\tt.Fatal(\"tooth profile missing\")\n-\tr",
    "eturn nil\n+\t// decad refuses trimmed free-form boundaries. Chord the same samples and\n+\t// derive th",
    "eir root intersections in a second solid-input sketch. The\n+\t// actual interpolating sketch above st",
    "ill proves the Fusion profile keys.\n+\tsection := toothSection(t, p)\n+\tprofiles := section.Profiles()",
    "\n+\tif len(profiles) != 1 || !profiles[0].Valid {\n+\t\tt.Fatalf(\"chorded tooth produced %d regions\", le",
    "n(profiles))\n+\t}\n+\treturn []*decad.Body{extrude(t, doc, section, profiles[0], p)}\n }\n \n func stepExt",
    "rudeBody(t *testing.T, doc *decad.Document, p map[string]float64) []*decad.Body {\n \ts := proofkit.Ne",
    "wSketch(t)\n \tbuildGearSketch(t, s, p)\n \tfor _, profile := range s.Profiles() {\n-\t\tif len(profile.Out",
    "er) == 2 {\n+\t\tif len(profile.Outer) == 1 {\n \t\t\treturn []*decad.Body{extrude(t, doc, s, profile, p)}\n",
    " \t\t}\n \t}\n-\tt.Fatal(\"two-arc disc profile missing\")\n+\tt.Fatal(\"root disc profile missing after verify",
    "ing its two tooth contacts\")\n \treturn nil\n+}\n+\n+func toothSection(t *testing.T, p map[string]float64",
    ") *sketch.Sketch {\n+\ts := proofkit.NewSketch(t)\n+\td := dimensions(p)\n+\tleft, right := involute.Flank",
    "s(d.Base, d.Tip, d.Pitch, p[\"teeth\"], int(p[\"samples\"]), p[\"angle\"])\n+\tleft, right = rootTrim(left, ",
    "d.Root), rootTrim(right, d.Root)\n+\torigin := s.CreatePoint(p[\"x\"], p[\"y\"])\n+\ts.Fix(origin)\n+\tpoint :",
    "= func(q involute.Pt) *sketch.Point {\n+\t\tv := s.CreatePoint(p[\"x\"]+q.X, p[\"y\"]+q.Y)\n+\t\ts.Fix(v)\n+\t\tr",
    "eturn v\n+\t}\n+\trightRoot := point(right[0])\n+\tprevious := rightRoot\n+\tfor _, q := range right[1:] {\n+",
    "\t\tnext := point(q)\n+\t\ts.CreateLine(previous, next)\n+\t\tprevious = next\n+\t}\n+\tleftTip := point(left[le",
    "n(left)-1])\n+\ts.CreateArc(origin, previous, leftTip)\n+\tprevious = leftTip\n+\tfor i := len(left) - 2; ",
    "i >= 0; i-- {\n+\t\tnext := point(left[i])\n+\t\ts.CreateLine(previous, next)\n+\t\tprevious = next\n+\t}\n+\ts.C",
    "reateArc(origin, rightRoot, previous)\n+\treturn s\n }\n \n func volume(t *testing.T, b *decad.Body) floa",
    "t64 {\n@@ -132,6 +165,7 @@\n \t\ta := float64(i) * 2 * math.Pi / p[\"teeth\"]\n \t\tleft, right := involute.F",
    "lanks(d.Base, d.Tip, d.Pitch, p[\"teeth\"], int(p[\"samples\"]), a)\n \t\tleft, right = rootTrim(left, d.Ro",
    "ot), rootTrim(right, d.Root)\n+\t\tleft, right = rootBlendChord(left, filletRadius(p)), rootBlendChord(",
    "right, filletRadius(p))\n \t\trightRoot := point(right[0])\n \t\tif first == nil {\n \t\t\tfirst = rightRoot\n@",
    "@ -175,6 +209,31 @@\n \t\treturn append([]involute.Pt{{X: a.X + u*dx, Y: a.Y + u*dy}}, points[i:]...)\n ",
    "\t}\n \treturn nil\n+}\n+\n+func filletRadius(p map[string]float64) float64 {\n+\td := dimensions(p)\n+\tretur",
    "n d.Root * (math.Pi/p[\"teeth\"] - 2*(math.Tan(p[\"pressure\"])-p[\"pressure\"])) * .9 / 2\n+}\n+\n+// A fill",
    "et can consume the short root stub or first chord before reaching the\n+// flank. decad cannot merge ",
    "those successive section rewrites. This solid-only\n+// substitute joins the root point directly to a",
    " later sample at least two\n+// fillet radii away. It retains the root location, tip, all later sampl",
    "es, and\n+// prescribed fillet radius, but changes the near-root flank approximation.\n+// The indepen",
    "dent Gear Profile proof retains every original sample and stub.\n+func rootBlendChord(points []involu",
    "te.Pt, radius float64) []involute.Pt {\n+\tif radius <= 0 || len(points) < 3 {\n+\t\treturn points\n+\t}\n+\t",
    "root := points[0]\n+\tfor i := 1; i < len(points); i++ {\n+\t\tq := points[i]\n+\t\tif math.Hypot(q.X-root.X",
    ", q.Y-root.Y) >= 2*radius || i == len(points)-1 {\n+\t\t\treturn append([]involute.Pt{root}, points[i:].",
    "..)\n+\t\t}\n+\t}\n+\treturn points\n }\n \n func completeBody(t *testing.T, doc *decad.Document, p map[string",
    "]float64) *decad.Body {\n@@ -244,10 +303,14 @@\n \n func stepRootFillets(t *testing.T, doc *decad.Docum",
    "ent, p map[string]float64) []*decad.Body {\n \tb := completeBody(t, doc, p)\n-\td := dimensions(p)\n-\trad",
    "ius := d.Root * (math.Pi/p[\"teeth\"] - 2*(math.Tan(p[\"pressure\"])-p[\"pressure\"])) * .9 / 2\n+\treturn [",
    "]*decad.Body{filletBody(t, b, p)}\n+}\n+\n+func filletBody(t *testing.T, b *decad.Body, p map[string]fl",
    "oat64) *decad.Body {\n+\td := dimensions(p)\n+\tradius := filletRadius(p)\n \tif radius <= 0 {\n-\t\treturn [",
    "]*decad.Body{b}\n+\t\treturn b\n \t}\n \tselector := decad.Edges(decad.Concave(), decad.ParallelTo(r3.Vec{Z",
    ": 1}))\n \tedges, err := selector.SelectEdges(b)\n@@ -289,22 +352,32 @@\n \tif blends != 2*int(p[\"teeth\"]",
    ") {\n \t\tt.Fatalf(\"fillet cylinders=%d want=%g at radius %g\", blends, 2*p[\"teeth\"], radius)\n \t}\n-\tretu",
    "rn []*decad.Body{result}\n+\treturn result\n }\n \n func stepBoreCut(t *testing.T, doc *decad.Document, p",
    " map[string]float64) []*decad.Body {\n-\tb := stepRootFillets(t, doc, p)[0]\n \tif p[\"bore\"] <= 0 {\n-\t\tr",
    "eturn []*decad.Body{b}\n-\t}\n-\ts := proofkit.NewSketch(t)\n-\tstepBoreProfile(t, s, p)\n-\ttool := extrude",
    "(t, doc, s, s.Profiles()[0], p)\n-\tbefore := volume(t, b)\n-\tcut, err := decad.Cut(b, tool)\n-\tif err !",
    "= nil {\n-\t\tt.Fatalf(\"through bore: %v\", err)\n-\t}\n+\t\treturn stepRootFillets(t, doc, p)\n+\t}\n+\t// The B",
    "oolean arranger refuses these many-arc scenes at its fixed segment\n+\t// budget. Model the same centr",
    "al through-hole in the section before extrusion.\n+\t// The bore is disjoint from the root blends, so ",
    "their operations commute.\n+\t// Compare against an independently built unbored gear; keep that refere",
    "nce\n+\t// in its own document rather than leaving an overlapping live body in doc.\n+\treference := ste",
    "pRootFillets(t, decad.New(), p)[0]\n+\tbefore := volume(t, reference)\n+\ts := completedSection(t, p)\n+\t",
    "drawBoreCircle(t, s, p)\n+\tvar annulus *sketch.Profile\n+\tfor _, profile := range s.Profiles() {\n+\t\tif",
    " len(profile.Holes) == 1 {\n+\t\t\tannulus = profile\n+\t\t}\n+\t}\n+\tif annulus == nil || !annulus.Valid {\n+\t",
    "\tt.Fatal(\"completed gear section has no valid through-bore region\")\n+\t}\n+\tcut := filletBody(t, extru",
    "de(t, doc, s, annulus, p), p)\n \twant := math.Pi * p[\"bore\"] * p[\"bore\"] * p[\"thickness\"] / 4\n \tif re",
    "moved := before - volume(t, cut); math.Abs(removed-want) > want*1e-5 {\n \t\tt.Fatalf(\"bore removed vol",
    "ume=%g want=%g\", removed, want)\n"
  ]
}
