{
  "schema": 1,
  "encoding": "utf8-chunks",
  "original_name": "round3-sketches_test.go-diff.txt",
  "original_sha256": "6300108bcc197a5abad4a423c570ffd7d2a34baaca128e42e9d299734561fadf",
  "original_bytes": 3727,
  "chunks": [
    "--- round2/sketches_test.go\n+++ round3/sketches_test.go\n@@ -160,11 +160,24 @@\n \t\tt.Fatalf(\"Gear Prof",
    "ile has %d regions, want exactly 2\", len(profiles))\n \t}\n \tdisc, tooth := 0, 0\n+\tvar discCircle, toot",
    "hCircle *sketch.Circle\n+\tvar rootContacts []float64\n \tfor _, p := range profiles {\n-\t\tarcs, lines, s",
    "plines := 0, 0, 0\n+\t\tarcs, lines, splines, circles := 0, 0, 0, 0\n+\t\tvar root *sketch.Circle\n+\t\tvar c",
    "uts []float64\n+\t\tcircleSpan := 0.0\n \t\tfor _, edge := range p.Outer {\n-\t\t\tswitch edge.Entity.(type) {",
    "\n-\t\t\tcase *sketch.Circle, *sketch.Arc:\n+\t\t\tswitch curve := edge.Entity.(type) {\n+\t\t\tcase *sketch.Cir",
    "cle:\n+\t\t\t\tcircles++\n+\t\t\t\tif root != nil && root != curve {\n+\t\t\t\t\tt.Fatal(\"a region uses more than on",
    "e solid circle\")\n+\t\t\t\t}\n+\t\t\t\troot = curve\n+\t\t\t\tcircleSpan += edge.TEnd - edge.TStart\n+\t\t\t\tcuts = app",
    "end(cuts, edge.TStart, edge.TEnd)\n+\t\t\tcase *sketch.Arc:\n \t\t\t\tarcs++\n \t\t\tcase *sketch.Line:\n \t\t\t\tline",
    "s++\n@@ -176,25 +189,64 @@\n \t\tif embedded {\n \t\t\twantLines = 0\n \t\t}\n-\t\tif p.Valid && len(p.Holes) == 0",
    " && arcs == 2 && splines == 0 && lines == 0 && len(p.Outer) == 2 {\n+\t\t// The arrangement coalesces a",
    "djacent fragments of the same source. Its\n+\t\t// disc therefore reports one full Circle, although the",
    " tooth's two real\n+\t\t// contacts divide that circle into the two arcs Fusion reports. Verify\n+\t\t// t",
    "hose contacts on the actual tooth below, using this same circle.\n+\t\tif p.Valid && len(p.Holes) == 0 ",
    "&& circles == 1 && arcs == 0 && splines == 0 && lines == 0 &&\n+\t\t\tlen(p.Outer) == 1 && math.Abs(circ",
    "leSpan-1) < 1e-9 {\n \t\t\tdisc++\n-\t\t}\n-\t\tif p.Valid && len(p.Holes) == 0 && arcs == 2 && splines == 2 &",
    "& lines == wantLines && len(p.Outer) == 4+wantLines {\n+\t\t\tdiscCircle = root\n+\t\t}\n+\t\t// The root arc ",
    "crossing +X has two engine fragments because its parameter\n+\t\t// range cannot wrap across 1->0. Canc",
    "el only paired seam endpoints; never\n+\t\t// discard a real flank/root junction to reach the desired c",
    "ount.\n+\t\tcontacts := unpairedCircleEnds(cuts)\n+\t\tif p.Valid && len(p.Holes) == 0 && arcs == 1 && spl",
    "ines == 2 && lines == wantLines &&\n+\t\t\t(circles == 1 || circles == 2) && len(p.Outer) == arcs+spline",
    "s+lines+circles &&\n+\t\t\tcircleSpan > 0 && circleSpan < .5 && len(contacts) == 2 {\n \t\t\ttooth++\n-\t\t}\n-\t",
    "}\n-\tif disc != 1 || tooth != 1 {\n+\t\t\ttoothCircle, rootContacts = root, contacts\n+\t\t}\n+\t}\n+\tif disc !",
    "= 1 || tooth != 1 || discCircle != toothCircle || len(rootContacts) != 2 {\n \t\tt.Fatalf(\"profile keys",
    ": discs=%d teeth=%d embedded=%t\", disc, tooth, embedded)\n \t}\n }\n \n+func unpairedCircleEnds(ends []fl",
    "oat64) []float64 {\n+\tvar unmatched []float64\n+\tfor _, end := range ends {\n+\t\tif end == 1 {\n+\t\t\tend =",
    " 0\n+\t\t}\n+\t\tfound := -1\n+\t\tfor i, old := range unmatched {\n+\t\t\tif old == 0 && end == 0 {\n+\t\t\t\tfound =",
    " i\n+\t\t\t\tbreak\n+\t\t\t}\n+\t\t}\n+\t\tif found < 0 {\n+\t\t\tunmatched = append(unmatched, end)\n+\t\t\tcontinue\n+\t\t}\n",
    "+\t\tunmatched = append(unmatched[:found], unmatched[found+1:]...)\n+\t}\n+\treturn unmatched\n+}\n+\n func s",
    "tepBoreProfile(t testing.TB, s *sketch.Sketch, p map[string]float64) {\n+\tdrawBoreCircle(t, s, p)\n+\ti",
    "f profiles := s.Profiles(); len(profiles) != 1 || len(profiles[0].Outer) != 1 {\n+\t\tt.Fatalf(\"bore pr",
    "ofile must have exactly one circle\")\n+\t}\n+}\n+\n+func drawBoreCircle(t testing.TB, s *sketch.Sketch, p",
    " map[string]float64) {\n \tproofkit.Step(t, \"project Tools anchor and ground the constructor's otherwi",
    "se unused local origin\")\n \tanchor := s.CreateReferencePoint(p[\"x\"], p[\"y\"], \"Tools.anchor\")\n \torigin",
    " := s.CreatePoint(p[\"x\"], p[\"y\"])\n \tcircle := s.CreateCircle(anchor, p[\"bore\"]/2)\n \ts.AddConstraint(",
    "sketch.NewDiameter(circle, p[\"bore\"]), sketch.NewCoincident(origin, anchor))\n-\tif profiles := s.Prof",
    "iles(); len(profiles) != 1 || len(profiles[0].Outer) != 1 {\n-\t\tt.Fatalf(\"bore profile must have exac",
    "tly one circle\")\n-\t}\n-}\n+}\n"
  ]
}
