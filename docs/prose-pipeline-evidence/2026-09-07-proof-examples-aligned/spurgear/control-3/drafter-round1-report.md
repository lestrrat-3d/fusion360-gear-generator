I wrote these drafts in `.worktrees/chore-prose-2026-09-07-proof-examples-aligned-control-3-spurgear`:

- `.tmp/spurgear.steps.md`
- `.tmp/spurgear-proof/sketches_test.go`
- `.tmp/spurgear-proof/solids_test.go`

The prompt hash matches. Gofmt passed. I did not stage files, generate registrations, or run operator gates.

The source leaves parameter bounds unspecified and requires degenerate zero-length stubs at exact base/root equality.
Its prefix descriptions conflict, and some step references are stale. Three required API calls remain advisory.
Dimension signatures use `isDriving`; the chamfer edge-set boolean uses `isTangentChain`, contrary to source terminology.
Final Fusion chamfer verification remains pending.

The proof records its solid substitutions and chamfer-selection limitation.

The operator must place the drafts, generate provenance and registrations, and run validation.
