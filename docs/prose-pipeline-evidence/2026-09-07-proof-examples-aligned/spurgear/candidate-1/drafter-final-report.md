I wrote these drafts in `.worktrees/chore-prose-2026-09-07-proof-examples-aligned-candidate-1-spurgear`:

- `.tmp/spurgear.steps.md` contains 13 timeline entries with version-2 metadata.
- `.tmp/spurgear-proof/sketches_test.go` contains the sketch proofs.
- `.tmp/spurgear-proof/solids_test.go` contains the solid proofs.

The saved prompt hash matched, and `gofmt` passed. I left registrations, placement and operator gates untouched.

The source gaps and proof limits are:

- The legacy bench permits ambiguity, while the required harness rejects it.
- Exact base/root equality produces zero-length stubs. The source provides no valid parameter bounds around that case.
- The introductory prefix and “annular body” wording conflict with the inherited prefix algorithm and explicit root-disc instruction.
- `Sketch.project`, `SketchTexts.createInput2` and `FilletFeatureInput.addConstantRadiusEdgeSet` have advisory API status.
- The solid proofs document chord substitutions. Root fillets and the completed-gear chamfer remain PROSE limits.
- The completed-gear chamfer still lacks final Fusion verification in the cited source.

The operator must scaffold registrations and validate the drafts.
