{
  "schema": 1,
  "encoding": "utf8-chunks",
  "original_name": "reported-source-passages.txt",
  "original_sha256": "a450661b78b48647b6ba1d50a41c121ea42b1397f529a66df71694f35a0872fe",
  "original_bytes": 9582,
  "chunks": [
    "spec/spurgear/instructions.md:37: User inputs are listed below in the order they appear in the comma",
    "nd dialog. Derived (calculated) parameters are registered as Fusion user parameters under the `SpurG",
    "ear<N>_` prefix so they are visible and editable after generation; they are listed after the inputs ",
    "they depend on.\n\nspec/spurgear/instructions.md:484: The 12 steps below are preceded by a dialog-read",
    "ing pass. The order matters for one specific reason: as soon as you call `parentComponent.occurrence",
    "s.addNewComponent(...)` (directly via `getOccurrence()`, or indirectly via the first `addParameter()",
    "` / `parameterName()` call), Fusion's active component context shifts to the newly created occurrenc",
    "e. `SelectionCommandInput`s holding entities that live in a *different* component — for example a `S",
    "ketchPoint` on a sketch in the root component, while the new gear is being added under the root — ca",
    "n drop their selections when that context shift happens. Numeric and boolean inputs are unaffected.\n",
    "\nspec/spurgear/instructions.md:499: ### 2: Tools Sketch\nspec/spurgear/instructions.md:500: \nspec/spu",
    "rgear/instructions.md:501: Create a sketch named `Tools` on the target plane. Project the Anchor Poi",
    "nt into it and keep the result as `ctx.anchorPoint` — this is the canonical handle every later sketc",
    "h will re-project from (see Sketch Discipline). The sketch draws no geometry of its own; it exists t",
    "o own this one reference. Leave it visible while the later sketches are still projecting from it; to",
    "ggle `isVisible = False` once the gear is fully built.\n\nspec/spurgear/instructions.md:578: - **`Gear",
    " Center` construction axis** — from any face whose `surfaceType` is `CylinderSurfaceType`. Build it ",
    "with `constructionAxes.createInput()` → `axisInput.setByCircularFace(cylindrical_face)` → `construct",
    "ionAxes.add(axisInput)`. Name it `Gear Center`; set `isLightBulbOn = False`. Store on `ctx.centerAxi",
    "s`.\n\nspec/spurgear/instructions.md:600: ### 12: Bore (optional)\nspec/spurgear/instructions.md:601: \n",
    "spec/spurgear/instructions.md:602: `buildBore` runs unconditionally from `generate()` (after `buildM",
    "ainGearBody`), so it MUST itself early-return in two cases: when **SketchOnly** is set, and when **B",
    "ore Diameter ≤ 0**. The SketchOnly guard is essential — in sketch-only mode `buildMainGearBody` shor",
    "t-circuits before `buildBody`, so `ctx.gearBody` and `ctx.extrusionExtent` are never set; proceeding",
    " into the cut would dereference `None`. (Do not rely on the bore diameter being 0 in sketch-only mod",
    "e — the user may have set both.)\nspec/spurgear/instructions.md:603: \nspec/spurgear/instructions.md:6",
    "04: Otherwise (full build, Bore Diameter > 0), create a separate `Bore Profile` sketch on the target",
    " plane and draw the bore circle **by instantiating the tooth generator on that sketch** — `SpurGearI",
    "nvoluteToothDesignGenerator(boreSketch, self)` — and calling its `drawBore(ctx.anchorPoint, boreDiam",
    "eter)`, which projects the anchor in and draws the construction-less circle of that diameter with a ",
    "driving diameter dimension. Note the accepted side effect: the tooth generator's **constructor** alw",
    "ays adds its local-origin `(0, 0, 0)` `SketchPoint` (see `[SPUR-F-LOCAL-ORIGIN]`), so the Bore Profi",
    "le sketch carries one stray unused sketch point at (0,0,0) — faithful behavior, don't suppress it. *",
    "*Ground that point on the projected anchor**, exactly as step 5 does for the Gear Profile sketch: `d",
    "rawBore` already projects `ctx.anchorPoint` into this sketch to place the circle's centre, so add `a",
    "ddCoincident(toothGen.anchorPoint, projectedAnchor)` using that same projection. The local origin th",
    "en rides on the anchor like every other sketch's does, the Bore Profile sketch is fully constrained,",
    " and the bore follows the anchor if the user moves it. Do **not** ground it on `boreSketch.originPoi",
    "nt` instead — that pins the point to the plane rather than the gear, and `[PB-CIRCLE-CENTER]` record",
    "s a solver failure from constraining to `originPoint`. Without any grounding the point is free in tw",
    "o directions and the sketch never reaches `isFullyConstrained`. Then extrude-cut the bore profile fr",
    "om the target plane to `ctx.extrusionExtent` (the far end-cap face), affecting only `ctx.gearBody`. ",
    "The `ToEntityExtentDefinition` to the far face guarantees the bore goes all the way through regardle",
    "ss of Thickness.\n\nspec/spurgear/sketch/README.md:133: exactly what a default 31/31 bevel pair draws.",
    "\nspec/spurgear/sketch/README.md:134: \nspec/spurgear/sketch/README.md:135: The Tools sketch carries n",
    "o geometry (just an anchor projection — nothing to constrain).\nspec/spurgear/sketch/README.md:136: <",
    "line does not exist>\n\nspec/spurgear/fusion.md:26: - **[SPUR-F-LOCAL-ORIGIN] Each sketch that must fo",
    "llow the anchor keeps its own movable local\nspec/spurgear/fusion.md:27:   origin.** That is a fresh ",
    "`SketchPoint` added at (0, 0, 0) — **not** `sketch.originPoint`, which\nspec/spurgear/fusion.md:28:  ",
    " is immutable and can't be coincident-constrained to anything brought in from elsewhere. The tooth\ns",
    "pec/spurgear/fusion.md:29:   generator draws all its geometry relative to this local origin (the fie",
    "ld `self.anchorPoint`),\nspec/spurgear/fusion.md:30:   then at the very end constrains it coincident ",
    "with the projected anchor (step 5); Fusion then\nspec/spurgear/fusion.md:31:   slides the whole sketc",
    "h onto the user's anchor as a unit.\n\n.claude/skills/generate-gear/PLAYBOOK.md:83: - `getOccurrence()",
    "` — lazily creates the child occurrence under `self.parentComponent`, and on\n.claude/skills/generate",
    "-gear/PLAYBOOK.md:84:   first creation builds the `ParamNamePrefix` as `f'{self.prefixBase()}_{compo",
    "nent.id without\n.claude/skills/generate-gear/PLAYBOOK.md:85:   dashes}'` and a `ComponentCleaner`. *",
    "*Calling this (or anything that calls it transitively,\n.claude/skills/generate-gear/PLAYBOOK.md:86: ",
    "  including `parameterName()`/`addParameter()`) shifts Fusion's active component context.**\n\n.claude",
    "/skills/generate-gear/PLAYBOOK.md:455:   - **[PB-PROJECT-NOT-FIXED] `sketch.project(...)` does NOT f",
    "ix the projected geometry.** A projected point/curve is brought\n.claude/skills/generate-gear/PLAYBOO",
    "K.md:456:     in *associatively* (it tracks its source) but still carries **free DOF** — it is a ref",
    "erence, not\n.claude/skills/generate-gear/PLAYBOOK.md:457:     a fixed point. A sketch whose geometry",
    " hangs off shared projected points therefore reports\n.claude/skills/generate-gear/PLAYBOOK.md:458:  ",
    "   **under-constrained**, even though every projected point already has a correct position (so\n.clau",
    "de/skills/generate-gear/PLAYBOOK.md:459:     `worldGeometry` looks fine and the feature builds — the",
    " defect is silent until you check\n.claude/skills/generate-gear/PLAYBOOK.md:460:     `isFullyConstrai",
    "ned`). To turn another sketch's points into *fully-constrained* local geometry,\n.claude/skills/gener",
    "ate-gear/PLAYBOOK.md:461:     either (a) `addCoincident` the projection to an already-fixed point (w",
    "orks when you have one\n.claude/skills/generate-gear/PLAYBOOK.md:462:     natural anchor, e.g. spur's",
    " tooth anchor), or (b) **recreate each as a brand-new point and fix it\n\n.claude/skills/generate-gear",
    "/PLAYBOOK.md:758: - **[PB-CONSTRUCTION-NEEDS-ACTIVE] Construction geometry (points/axes/planes) need",
    "s an ACTIVE component — sketch geometry does\n.claude/skills/generate-gear/PLAYBOOK.md:759:   not.** ",
    "`component.constructionPoints.add(...)` / `constructionAxes.add(...)` raise `RuntimeError:\n.claude/s",
    "kills/generate-gear/PLAYBOOK.md:760:   3 : Environment is not supported` when `component` is not the",
    " activated one. Sketches, solids, and\n.claude/skills/generate-gear/PLAYBOOK.md:761:   features happi",
    "ly build on a non-activated component, but construction geometry does not — and the\n.claude/skills/g",
    "enerate-gear/PLAYBOOK.md:762:   cross-gear rule is to **never** activate (activating mis-resolves ex",
    "ternally-selected planes onto\n.claude/skills/generate-gear/PLAYBOOK.md:763:   world XY). So when you",
    "'d reach for a construction point/axis, prefer geometry that needs no active\n.claude/skills/generate",
    "-gear/PLAYBOOK.md:764:   component: use a **`SketchPoint`** as a loft point-section (not a `Construc",
    "tionPoint`), and feed\n.claude/skills/generate-gear/PLAYBOOK.md:765:   rotations a `Matrix3D` axis fr",
    "om a sketch edge's `worldGeometry` (not a `ConstructionAxis`). Reserve\n.claude/skills/generate-gear/",
    "PLAYBOOK.md:766:   construction geometry for code paths that genuinely run in the active/root compon",
    "ent.\n\n.claude/skills/generate-gear/PLAYBOOK.md:788: - **[PB-NEVER-ACTIVATE] NEVER call `occurrence.a",
    "ctivate()`.** Build in a component by calling its own collections\n.claude/skills/generate-gear/PLAYB",
    "OOK.md:789:   (`comp.sketches.add(...)`, `comp.features.*`) on the *non-activated* component — exact",
    "ly as a\n.claude/skills/generate-gear/PLAYBOOK.md:790:   single-gear generator does. Activating a sub",
    "-occurrence and then sketching on a plane/face the\n.claude/skills/generate-gear/PLAYBOOK.md:791:   *",
    "*user selected in another component** (root) makes Fusion resolve that external reference in the\n.cl",
    "aude/skills/generate-gear/PLAYBOOK.md:792:   activated component's **local frame** (identity → world",
    "-XY-aligned), silently collapsing the\n.claude/skills/generate-gear/PLAYBOOK.md:793:   build's orient",
    "ation onto XY no matter what plane the user picked. This is a real, hard-to-see\n.claude/skills/gener",
    "ate-gear/PLAYBOOK.md:794:   trap: the gear still builds, just flat on XY. A single-gear generator th",
    "at respects arbitrary\n.claude/skills/generate-gear/PLAYBOOK.md:795:   planes works precisely *becaus",
    "e* it never activates — match that.\n"
  ]
}
