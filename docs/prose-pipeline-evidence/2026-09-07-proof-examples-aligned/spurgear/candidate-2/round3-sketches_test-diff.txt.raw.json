{
  "schema": 1,
  "encoding": "utf8-chunks",
  "original_name": "round3-sketches_test-diff.txt",
  "original_sha256": "b6b0c9fdcc0f23bbc8f901ab3be8ce83c1c68a5d9848a89b2093810292a9d9d8",
  "original_bytes": 2467,
  "chunks": [
    "--- round2/proof/sketches_test.go\n+++ round3/proof/sketches_test.go\n@@ -179,18 +179,72 @@\n \t\twantLin",
    "es = 0\n \t}\n \tvar teeth, discs int\n+\tvar tooth *sketch.Profile\n \tfor _, profile := range profiles {\n ",
    "\t\trequire.True(t, profile.Valid)\n \t\tlines, arcs, splines := profileKinds(profile)\n \t\tif lines == wan",
    "tLines && arcs == 2 && splines == 2 {\n \t\t\tteeth++\n-\t\t}\n-\t\tif lines == 0 && arcs == 2 && splines == 0",
    " {\n+\t\t\ttooth = profile\n+\t\t}\n+\t\tif rootDisk(profile) {\n \t\t\tdiscs++\n+\t\t\trequire.InDelta(t, math.Pi*d.R",
    "oot*d.Root, profile.Area, 1e-8)\n \t\t}\n \t}\n \trequire.Equal(t, 1, teeth, \"expected one tooth loop with ",
    "two flanks and two arcs\")\n-\trequire.Equal(t, 1, discs, \"expected the root disk boundary split into e",
    "xactly two arcs\")\n+\trequire.Equal(t, 1, discs, \"expected one intact root disk\")\n+\tassertRootContacts",
    "(t, tooth)\n+}\n+\n+// The engine preserves a closed primitive's disk as one circle even when the\n+// n",
    "eighboring tooth region uses a partial arc of it. Fusion reports that disk\n+// as two arcs. Check th",
    "e two actual region contacts below rather than guessing\n+// Fusion's segmentation from the engine's ",
    "primitive-preserving disk record.\n+func rootDisk(profile *sketch.Profile) bool {\n+\tif len(profile.Ho",
    "les) != 0 || len(profile.Entities) != 1 {\n+\t\treturn false\n+\t}\n+\t_, ok := profile.Entities[0].(*sketc",
    "h.Circle)\n+\treturn ok\n+}\n+\n+func assertRootContacts(t testing.TB, tooth *sketch.Profile) {\n+\tt.Helpe",
    "r()\n+\trequire.NotNil(t, tooth)\n+\tvar parameters []float64\n+\tvar counts []int\n+\tfor _, edge := range ",
    "tooth.Outer {\n+\t\tif _, ok := edge.Entity.(*sketch.Circle); !ok {\n+\t\t\tcontinue\n+\t\t}\n+\t\tfor _, paramet",
    "er := range []float64{edge.TStart, edge.TEnd} {\n+\t\t\tparameter = math.Mod(parameter, 1)\n+\t\t\tindex := ",
    "-1\n+\t\t\tfor i, previous := range parameters {\n+\t\t\t\tif math.Abs(previous-parameter) < 1e-10 {\n+\t\t\t\t\tin",
    "dex = i\n+\t\t\t\t\tbreak\n+\t\t\t\t}\n+\t\t\t}\n+\t\t\tif index < 0 {\n+\t\t\t\tparameters = append(parameters, parameter)\n",
    "+\t\t\t\tcounts = append(counts, 1)\n+\t\t\t} else {\n+\t\t\t\tcounts[index]++\n+\t\t\t}\n+\t\t}\n+\t}\n+\tvar contacts int\n",
    "+\tfor _, count := range counts {\n+\t\tif count%2 == 1 {\n+\t\t\tcontacts++\n+\t\t}\n+\t}\n+\t// An artificial t=0",
    "/1 seam cancels in pairs. The two remaining endpoints\n+\t// are measured boundaries of the real tooth",
    " profile on the real root circle;\n+\t// they divide the full disk boundary into the two arcs Fusion s",
    "elects.\n+\trequire.Equal(t, 2, contacts, \"actual root-circle contacts must divide the disk into two a",
    "rcs\")\n }\n \n func profileKinds(p *sketch.Profile) (int, int, int) {\n"
  ]
}
