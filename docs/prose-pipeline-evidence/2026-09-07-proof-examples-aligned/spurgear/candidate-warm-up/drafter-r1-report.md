I wrote the draft in
`.worktrees/chore-prose-2026-09-07-proof-examples-aligned-candidate-warm-up-spurgear`:

- `.tmp/spurgear.steps.md` contains 16 steps.
- `.tmp/spurgear-proof/sketches_test.go` contains the sketch proof.
- `.tmp/spurgear-proof/solids_test.go` contains the solid proof.

The prompt digest is unchanged. `gofmt` passed. I did not generate registrations or run operator gates.

Source gaps include unspecified valid parameter bounds, degenerate base/root equality, construction-axis
activation requirements, and parent-preselection lifecycle restrictions. API wording also differs for
`isDriving`, chamfer tangent chaining, and text character spacing. Three API methods remain advisory:
`Sketch.project`, `SketchTexts.createInput2`, and `FilletFeatureInput.addConstantRadiusEdgeSet`.

The proof documents chorded geometry substitutions. The completed-gear chamfer remains a best attempt requiring
validation; convexity-based exclusion may also omit concave root-cap edges.

The operator must generate registrations and validate the draft.
