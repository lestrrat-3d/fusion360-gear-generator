I wrote the drafts in `.worktrees/chore-prose-2026-09-07-proof-examples-aligned-control-2-spurgear`:

- `.tmp/spurgear.steps.md`
- `.tmp/spurgear-proof/sketches_test.go`
- `.tmp/spurgear-proof/solids_test.go`

The saved prompt hash remains unchanged. Go formatting passed. I created no registrations and ran no operator gates.

The report retains these limits:

- The source does not specify whether a selected SketchPoint must already be constrained.
- Exact base/root equality creates the specified zero-length stub; no safe transition tolerance is supplied.
- Three API members remain advisory: `Sketch.project`, `SketchTexts.createInput2`, and
  `FilletFeatureInput.addConstantRadiusEdgeSet`.
- The solid proof uses documented chord substitutions. Fusion spline interpolation, component frames and timeline
  associations remain outside its scope.
- The earlier ambiguity concern describes different gates, not a confirmed source contradiction.

The operator must validate the drafts.
