{
  "schema": 1,
  "encoding": "utf8-chunks",
  "original_name": "round3-sketches_test.go-diff.txt",
  "original_sha256": "9ae27b9fe95f8f49e4f730857c6ad4be440f855120f58124ddec647bedee1414",
  "original_bytes": 2678,
  "chunks": [
    "--- round2/sketches_test.go\n+++ round3/sketches_test.go\n@@ -3,6 +3,7 @@\n import (\n \t\"fmt\"\n \t\"math\"\n+",
    "\t\"sort\"\n \t\"testing\"\n \n \t\"github.com/lestrrat-3d/fusion360-gear-generator/proof/involute\"\n@@ -199,14 ",
    "+200,24 @@\n \t\tt.Fatalf(\"Gear Profile closes %d regions, expected exactly two\", len(profiles))\n \t}\n \t",
    "var tooth, disc *sketch.Profile\n+\tvar rootCuts []float64\n \tfor _, p := range profiles {\n \t\tlines, ar",
    "cs, splines := 0, 0, 0\n+\t\trootCoverage := 0.0\n+\t\tvar cuts []float64\n \t\tfor _, edge := range p.Outer ",
    "{\n \t\t\tswitch edge.Entity.(type) {\n \t\t\tcase *sketch.Line:\n \t\t\t\tlines++\n-\t\t\tcase *sketch.Circle, *sket",
    "ch.Arc:\n+\t\t\tcase *sketch.Arc:\n \t\t\t\tarcs++\n+\t\t\tcase *sketch.Circle:\n+\t\t\t\trootCoverage += edge.TEnd - ",
    "edge.TStart\n+\t\t\t\tfor _, at := range []float64{edge.TStart, edge.TEnd} {\n+\t\t\t\t\tif at > 1e-8 && at < 1",
    "-1e-8 {\n+\t\t\t\t\t\tcuts = append(cuts, at)\n+\t\t\t\t\t}\n+\t\t\t\t}\n \t\t\tcase *sketch.FitSpline:\n \t\t\t\tsplines++\n \t\t",
    "\tdefault:\n@@ -216,16 +227,44 @@\n \t\tif !p.Valid || len(p.Holes) != 0 {\n \t\t\tt.Fatalf(\"invalid or holed",
    " gear region: %+v\", p)\n \t\t}\n+\t\t// A circle's artificial +X parameter seam divides a contiguous arc\n+",
    "\t\t// into two BoundaryEdges. Conversely, the disc's entire boundary may\n+\t\t// be one circle edge bec",
    "ause external dangling attachments are pruned.\n+\t\t// Count the two real root intersections, not that",
    " incidental seam.\n+\t\tif rootCoverage > 0 {\n+\t\t\tarcs++\n+\t\t}\n \t\tif splines == 2 && arcs == 2 && ((!emb",
    "edded && lines == 2) || (embedded && lines == 0)) {\n \t\t\ttooth = p\n-\t\t} else if splines == 0 && arcs ",
    "== 2 && lines == 0 {\n+\t\t\tif rootCoverage >= 1 {\n+\t\t\t\tt.Fatal(\"tooth root boundary must be a proper c",
    "ircle fragment\")\n+\t\t\t}\n+\t\t\trootCuts = cuts\n+\t\t} else if splines == 0 && arcs == 1 && lines == 0 {\n \t",
    "\t\tdisc = p\n+\t\t\tcloseValue(t, \"disc complete root boundary\", rootCoverage, 1, 1e-7)\n \t\t} else {\n \t\t\tt",
    ".Fatalf(\"profile counts lines=%d arcs=%d splines=%d embedded=%v\", lines, arcs, splines, embedded)\n \t",
    "\t}\n \t}\n \tif tooth == nil || disc == nil {\n \t\tt.Fatal(\"the tooth and two-arc disc profile must each e",
    "xist\")\n+\t}\n+\tsort.Float64s(rootCuts)\n+\tvar distinct []float64\n+\tfor _, cut := range rootCuts {\n+\t\tif",
    " len(distinct) == 0 || math.Abs(cut-distinct[len(distinct)-1]) > 1e-7 {\n+\t\t\tdistinct = append(distin",
    "ct, cut)\n+\t\t}\n+\t}\n+\tif len(distinct) != 2 {\n+\t\tt.Fatalf(\"root circle has %d real contact vertices, e",
    "xpected two: %v\", len(distinct), distinct)\n+\t}\n+\t// Those two detected vertices divide the disc's ac",
    "tual full circle into\n+\t// exactly two nonzero arcs. No additional boundary geometry is authored.\n+\t",
    "firstArc := distinct[1] - distinct[0]\n+\tif firstArc <= 0 || firstArc >= 1 {\n+\t\tt.Fatal(\"root contact",
    "s do not split the circle into two nonzero arcs\")\n \t}\n \treturn tooth, disc\n }\n"
  ]
}
